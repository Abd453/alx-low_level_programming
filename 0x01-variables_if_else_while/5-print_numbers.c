#include <stdio.h>

/**
 * main - entry point
 *
 * Desciption: print all single digit numbers
 *
 * Return: ALways 0 (success)
 */

int main(void)
{
	int digit = 0;

	while (ch <= 'z')
	{
		printf("%i", digit);
		digit++;
	}
	putchar("\n");

	return (0);
}

